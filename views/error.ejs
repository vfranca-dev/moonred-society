<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof pageTitle !== 'undefined' ? pageTitle : 'Error' %></title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* Estilos copiados de coming-soon para consistência, mas sem layout-container */
        body { display: flex; justify-content: center; align-items: center; min-height: 100vh; /* Alterado */ text-align: center; flex-direction: column; background-color: var(--bg-color-main); color: var(--text-color-main); font-family: var(--font-primary); font-size: var(--font-size-base); line-height: 1.3; }
        .error-container { background-color: var(--bg-color-content-box); padding: 2.5rem 3.5rem; /* Padding aumentado */ border-radius: 5px; border: 1px solid var(--border-color-main); max-width: 650px; /* Pouco maior */ width: 90%; }
        .error-code { font-size: var(--font-size-display); /* Usa variável */ color: #d10000; margin-bottom: 1rem; font-family: var(--font-display); }
        .error-container h2 { font-family: var(--font-display); font-size: var(--font-size-xl); color: var(--text-color-headings); margin-bottom: 1.5rem; }
        .error-message { font-size: var(--font-size-large); color: var(--text-color-main); margin-bottom: 2rem; }
        a.back-link { display: inline-block; margin-top: 1.5rem; color: var(--text-color-subtle); text-decoration: none; border: 1px solid var(--border-color-input); padding: 0.8rem 1.5rem; border-radius: 3px; transition: all 0.2s; font-family: var(--font-navigation); font-size: var(--font-size-normal); }
        a.back-link:hover { background-color: var(--bg-color-item-header-hover); border-color: var(--border-color-highlight); color: var(--text-color-sidebar-link-active); }
    </style>
</head>
<body> <!-- Sem layout-container -->
    <div class="error-container">
        <div class="error-code"><%= typeof errorCode !== 'undefined' ? errorCode : 'Error' %></div>
        <h2><%= typeof pageTitle !== 'undefined' ? pageTitle : 'An Error Occurred' %></h2>
        <p class="error-message"><%= typeof errorMessage !== 'undefined' ? errorMessage : 'Something went wrong.' %></p>
        <a href="/" class="back-link">← Go Home</a>
    </div>

    <!-- Sem Footer aqui -->

    <!-- Scripts provavelmente não necessários aqui -->
    <!-- <script src="/js/theme-switcher.js" defer></script> -->
    <!-- etc. -->
</body>
</html>