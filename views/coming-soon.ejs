<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof pageTitle !== 'undefined' ? pageTitle : 'Coming Soon' %> - Moonred Society</title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* Estilos específicos para centralizar */
        .coming-soon-content {
            text-align: center;
            padding: 4rem 1rem;
            color: var(--text-color-subtle);
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        .coming-soon-content h2 {
            font-family: var(--font-display); /* Gothferatu */
            font-size: var(--font-size-xxl); /* Tamanho maior */
            color: var(--text-color-headings);
            margin-bottom: 1.5rem;
        }
         .coming-soon-content p {
             font-size: var(--font-size-large); /* Tamanho maior */
             color: var(--text-color-main);
             margin-bottom: 2.5rem;
         }
          a.back-button { /* Classe consistente */
             display: inline-block;
             margin-top: 1.5rem;
             color: var(--text-color-subtle);
             text-decoration: none;
             border: 1px solid var(--border-color-input);
             padding: 0.8rem 1.5rem; /* Padding maior */
             border-radius: 3px;
             transition: all 0.2s;
             font-family: var(--font-primary); /* Usa a variável correta */
             font-size: var(--font-size-normal);
         }
         a.back-button:hover {
             background-color: var(--bg-color-item-header-hover);
             border-color: var(--border-color-highlight);
             color: var(--text-color-sidebar-link-active);
         }
    </style>
</head>

<body> <!-- SEM classes iniciais -->

    <!-- REMOVIDO: Modal de Seleção Inicial de View -->
    <!-- <div id="initial-view-modal" class="initial-view-modal hidden"> ... </div> -->

    <!-- Botão Hamburger e Overlay -->
    <button id="mobile-nav-toggle" class="mobile-nav-toggle" aria-label="Toggle navigation" aria-expanded="false">
        <span class="icon-open">☰</span>
        <span class="icon-close">✕</span>
    </button>
    <div id="mobile-overlay" class="mobile-overlay"></div>
    <!-- Fim do Bloco Adicionado -->

    <div class="layout-container">
        <%- include('./partials/sidebar') %> <!-- Sidebar Principal -->

        <main class="content">
            <%- include('./partials/header') %>
            <%- include('./partials/messages') %>

            <div class="coming-soon-content">
                <h2><%= typeof sectionTitle !== 'undefined' ? sectionTitle : pageTitle %> Coming Soon...</h2>
                <p>This section is currently under development. Stay tuned!</p>
                 <a href="/" class="back-button">← Go Home</a>
            </div>

            <%- include('./partials/footer') %>

        </main>
    </div>

    <!-- Scripts Globais -->
    <script src="/js/script.js" defer></script>
    <script src="/js/dropdown.js" defer></script>
    <script src="/js/theme-switcher.js" defer></script>
    <script src="/js/view-switcher.js" defer></script>

</body>
</html>